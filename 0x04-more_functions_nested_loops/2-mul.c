#include "main.h"
#include <stdio.h>

/**
 * mul - Checks if an alphabet is upper or not
 * @a: The integer that multiply b
 * @b: The integer that multiply a
 *
 * Return: Product of a and b
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
